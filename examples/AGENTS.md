# examples AGENTS

## Purpose
Runnable demos for the public SDK surface.

## What exists now
- `basic/` — minimal one-shot client flow using `Run`.

## What is missing
- Session-client example (`StartSession`, `ExportHTML`, `ShareSession`).
- Streaming/subscription example with policy modes.

## Owned files
- `basic/main.go`

## Canonical commands
- `go run ./examples/basic`
- `go test ./...`
